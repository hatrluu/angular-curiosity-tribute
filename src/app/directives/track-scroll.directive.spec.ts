import { TrackscrollDirective } from './track-scroll.directive';

describe('TrackscrollDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackscrollDirective();
    expect(directive).toBeTruthy();
  });
});
